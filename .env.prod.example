# Production env for docker-compose.prod.yml
# Copy to ".env" next to docker-compose.prod.yml and set a strong password.
# Generate one with: openssl rand -hex 24

DB_PASSWORD=change-me-to-a-strong-random-secret
