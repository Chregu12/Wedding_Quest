{
  "name": "wedding-quest-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "API-level e2e tests for the Wedding Quest microservices (Playwright APIRequestContext, no browser).",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/ws": "^8.5.12",
    "ws": "^8.18.0"
  }
}
